apiVersion: v1
kind: Service
metadata:
  name: {{ include "redis-rdb-analyzer.fullname" . }}
  labels:
    {{- include "redis-rdb-analyzer.labels" . | nindent 4 }}
  {{- with .Values.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "redis-rdb-analyzer.selectorLabels" . | nindent 4 }}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ include "redis-rdb-analyzer.fullname" . }}-headless
  labels:
    {{- include "redis-rdb-analyzer.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  clusterIP: None
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "redis-rdb-analyzer.selectorLabels" . | nindent 4 }}
