# Redis RDB Analyzer Configuration
# Copy this file to .env for local development
# These environment variables are automatically set by Kubernetes ConfigMap in production

# Server Configuration
# Port for the web server to listen on
RDR_PORT=8080

# Kubernetes Configuration
# Pod discovery cache duration (reduces API calls to Kubernetes)
# Format: duration string (e.g., "15m", "1h", "30s")
POD_CACHE_DURATION=15m

# Analysis Configuration
# Maximum RDB file size to analyze
# Format: size string (e.g., "10Gb", "5Gb", "500Mb")
# Larger files will be rejected to prevent memory issues
MAX_RDB_SIZE=10Gb
